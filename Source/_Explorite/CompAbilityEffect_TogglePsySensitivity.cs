/**
 * 半人马切换自身心灵敏度的代码草稿废案。
 * 
 * --siiftun1857
 */
using UnityEngine;
using Verse;
using RimWorld;
using System;
using System.Collections.Generic;
using System.Runtime.CompilerServices;
using static Explorite.ExploriteCore;

namespace Explorite
{
    /*public class Ability_SensitivityToggle : Ability
    {
        public Ability_SensitivityToggle()
        { }
        public Ability_SensitivityToggle(Pawn pawn)
        { }
        public Ability_SensitivityToggle(Pawn pawn, AbilityDef def)
        { }
    }

    public class Verb_SensitivityToggle : Verb
    {
        protected override bool TryCastShot()
        {
            //Hediff hediff = HediffMaker.MakeHediff(HyperManipulatorHediffDef, CasterPawn, null);
            //CasterPawn?.health.AddHediff();
            return true;
        }
    }
    public class CompAbilityEffect_TogglePsySensitivity : CompAbilityEffect
    {

        public override void Apply(LocalTargetInfo target, LocalTargetInfo dest)
        {
            Pawn targetPawn;
            if (target.IsValid && target.Pawn != null)
                targetPawn = target.Pawn;
            else
                return;

            //if (targetPawn.health.hediffSet.HasHediff())
            //if (targetPawn.psychicEntropy.EntropyValue > 0f)
//
            //targetPawn.health.AddHediff("");
        }
        public bool Valid(LocalTargetInfo target, bool throwMessages = false)
        {
            return target.Thing.def == AlienCentaurDef;
        }
    }*/



}
